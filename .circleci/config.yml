version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:latest
    steps:
      - checkout
      - run:
          name: Gem setup
          command: |
            gem update --system
            gem install rubocop
      - run:
          name: Rubocop run
          command: rubocop --config .config/.rubocop.yml
workflows:
  version: 2
  build:
    jobs:
      - build
