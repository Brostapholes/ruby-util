version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:latest
      - checkout
      - run:
          command: |
            gem update --system
            gem install rubocop
            rubocop
workflows:
  version: 2
  build:
    jobs:
      - build
